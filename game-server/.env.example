# Server port and host configuration
GAME_SERVER_PORT=8001
GAME_SERVER_HOST=localhost
PUBLIC_SERVER_URL=http://localhost:8001

# Backend server connection
BACKEND_URL=http://localhost:8000/api

# Server-to-server API key (must match the key in backend-server)
SERVER_API_KEY=your-secure-server-api-key
